# OpenIM Coolify 部署环境变量配置文件
# 复制此文件为 .env 并根据您的服务器实际配置修改

# =============================================================================
# 服务器基础配置
# =============================================================================
SERVER_IP=0.0.0.0
DOMAIN=your-domain.com

# =============================================================================
# MongoDB 数据库配置 (使用已配置的服务器数据库)
# =============================================================================
MONGO_HOST=mongodb-host
MONGO_PORT=27017
MONGO_DATABASE=openim_v3
MONGO_USERNAME=openIM
MONGO_PASSWORD=openIM123

# =============================================================================
# Redis 缓存配置 (使用已配置的服务器 Redis)
# =============================================================================
REDIS_HOST=redis-host
REDIS_PORT=6379
REDIS_PASSWORD=openIM123

# =============================================================================
# Kafka 消息队列配置 (使用已配置的服务器 Kafka)
# =============================================================================
KAFKA_HOST=kafka-host
KAFKA_PORT=9092
# 如果 Kafka 启用了认证，请设置以下参数
KAFKA_USERNAME=
KAFKA_PASSWORD=

# =============================================================================
# etcd 服务发现配置 (使用已配置的服务器 etcd)
# =============================================================================
ETCD_HOST=etcd-host
ETCD_PORT=2379
# 如果 etcd 启用了认证，请设置以下参数
ETCD_USERNAME=
ETCD_PASSWORD=

# =============================================================================
# MinIO 对象存储配置 (使用已配置的服务器 MinIO)
# =============================================================================
MINIO_HOST=minio-host
MINIO_PORT=9000
MINIO_ACCESS_KEY=root
MINIO_SECRET_KEY=openIM123
MINIO_BUCKET=openim
MINIO_EXTERNAL_URL=https://minio.your-domain.com

# =============================================================================
# OpenIM 应用配置
# =============================================================================
OPENIM_SECRET=openIM123
OPENIM_ADMIN_USERID=imAdmin
OPENIM_ADMIN_NICKNAME=superAdmin

# =============================================================================
# 端口配置
# =============================================================================
# API 服务端口
API_PORT=10002
# WebSocket 网关端口
WEBSOCKET_PORT=10001
# 监控端口
PROMETHEUS_PORT=19090

# =============================================================================
# 日志和调试配置
# =============================================================================
LOG_LEVEL=info
DEBUG_MODE=false

# =============================================================================
# Coolify 部署配置
# =============================================================================
# 健康检查路径
HEALTH_CHECK_PATH=/api/get_server_api_map
# 容器重启策略
RESTART_POLICY=unless-stopped